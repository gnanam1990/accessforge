# Handoff 06 — Versioned journey DSL, fixtures and assertions

**Result: COMPLETE** for authoring and compilation. These are **specifications**, not executed
accessibility proof: nothing here has driven a screen reader, and an assertion describing an expected
announcement is a statement of what must be observed, not a record of it having been observed.

## Scope and exact state

| Item | Value |
|---|---|
| Branch | `feat/m06-journey-dsl` |
| Base SHA | `9a1aef6` |
| Dependencies | modules 02 and 05 |
| Schema | migration `0007_journeys_and_fixtures.sql` |

## Owned and changed paths

- `packages/domain/src/accessforge_domain/journeys/` — `assertions`, `dsl`, `validation`, `compile`
- `packages/persistence/src/accessforge_persistence/fixtures.py` — per-run instances
- migration `0007`
- `tests/unit/test_journey_dsl.py`, `tests/integration/test_fixture_instances.py`

## The E0 journey and its generated digests

One scenario: form-error recovery. Submit an invalid email, encounter the announced validation error,
correct the named field, create exactly one request.

Compilation produces four digests, deterministically — the same reviewed draft yields the same values
every time, which is what lets a baseline and a candidate be shown to share their journey exactly:

```
journeyDigest            over name, platform, intent, the three digests below, effects and budget
assertionSetDigest       over the ordered assertions, each with its observer and unknown reasons
fixtureDigest            over template, navigator values, and digests of reset/observer material
navigatorPolicyDigest    over exactly what the navigator may see and do
```

**What is deliberately absent from the journey: an expected action sequence.** A journey specifying
keystrokes would be a replay of a known-good path, and passing it would say nothing about whether a
real reader user could accomplish the task.

## Protected assertions

| Kind | Decided by | Why only that observer |
|---|---|---|
| TASK_COMPLETION | APPLICATION_OBSERVER | A phrase announced on screen is not evidence a row was written |
| REQUIRED_ANNOUNCEMENT | READER | A backend row proves nothing about what was spoken |
| FOCUS_BEHAVIOUR | READER | Only the reader sees focus |
| READING_ORDER | READER | Likewise |
| FORBIDDEN_EFFECT | EFFECT_MONITOR | Its true value is absence |
| FUNCTIONAL_VALIDATION | FUNCTIONAL_TEST | Makes INV-16 checkable: a repair that loosened validation fails here |

Each kind maps to exactly one observer. A kind decidable by two would let the weaker answer for the
stronger.

**Every required assertion must declare how it can be unknown.** A condition that can only be true or
false cannot represent a missing observation, which is precisely how an unobserved assertion becomes a
reported defect. Authoring refuses it (INV-02 at authoring time).

A journey must include a required TASK_COMPLETION assertion; marking it optional does not satisfy the
rule. Required completion is a frozen condition, not an optional extra.

## Capability validation results

| Input | Code |
|---|---|
| `linux` platform | `UNSUPPORTED_PLATFORM` |
| `CLICK_AT_COORDINATES` | `UNSUPPORTED_ACTION` |
| `CMD+T`, `CMD+L`, `CMD+OPT+I`, `CMD+C`, `CMD+Q`, `CTRL+ALT+DELETE`, `F12` | `FORBIDDEN_KEY_CHORD` |
| Windows chord on macOS | `FORBIDDEN_KEY_CHORD` — allowlists are per platform, not a union |
| Chords listed without `KEY_CHORD` | `INCONSISTENT_CAPABILITY` |
| `TYPE_TEXT` with no fixture values | `INCONSISTENT_CAPABILITY` |
| `SEND_EMAIL`, `TAKE_PAYMENT`, `SUBMIT_APPLICATION`, `PUBLISH_TO_GITHUB` | `FORBIDDEN_EFFECT` |
| `max_actions=10000` or `wall_time=86400` | refused as unbounded |
| No actions at all | `NO_ACTIONS` |

## Negative tests from the module's list

| Case | Result |
|---|---|
| Agent edits a protected assertion | frozen dataclass raises; a changed set changes the digest |
| Hidden selector in the task payload | refused — `#id`, `.class`, `[data-testid=]`, xpath, `querySelector` |
| Secret in a navigator fixture value | refused — AWS keys, GitHub tokens, PEM, `password:` |
| Receipt material reaching the navigator | observer config cannot share a key with navigator values; the policy contains neither it nor reset material |
| Unsupported chord escaping the browser | refused per platform |
| Copied announcement text as a forged observation | the phrase lives only in reviewer-facing assertion text, absent from the policy and fixture values |
| Required assertion disappears in a version update | digest changes, so it cannot pass as the same journey |
| Fixture reset reusing a prior receipt | instance digest includes a per-run nonce; one instance per run enforced by constraint |
| Unbounded deadline or budget | refused |

## Protected interfaces for modules 07, 12 and 11

- **Runner control (07):** `compile_journey(...).version` carries the four digests for sealing;
  `navigator_policy` is the sealed statement of permitted actions, chords and budgets.
- **Navigator (12):** `navigator_policy` and `fixtures.navigator_values` are the *only* inputs. The
  policy names its own prohibitions, and `navigator_values` has no parameter through which oracle
  material could be requested — a single reader with an `include_oracle` flag would be one careless
  call from handing over the answer key.
- **Outcome verification (11):** `AssertionSet.required` plus each assertion's `observer` and
  `unknown_reasons` feed the precedence rules directly.

## Mutation checks

| Mutation | Result |
|---|---|
| Selector detection removed | 7 tests fail |
| Secret detection removed | 5 tests fail |
| Required-assertion unknown-reason rule removed | 1 test fails |
| Completion assertion no longer required | 2 tests fail |
| Navigator policy merged with observer config | 1 test fails |

## A silent collision worth recording

The product's fixture table was first written as `fixture_instance` — the same name the reference
application under test already uses, in the same local database. `CREATE TABLE IF NOT EXISTS` did
nothing, silently, and the product would have been reading the application's rows. Renamed to
`run_fixture_instance`, with a test asserting both tables exist separately and that the product's is
the one with tenant scoping.

## Also worth recording: scripted line-wrapping damage

Trying to fix long lines with a script that did not understand Python broke string literals and
comment continuations across several files, including already-committed ones. Caught by a syntax check
and a failing suite, then reverted with `git checkout` for committed files and fixed by hand for the
new ones. This is the second time in two modules; line wrapping is a manual edit.

## Commands executed

| Command | Result |
|---|---|
| `ruff format --check` / `ruff check` | clean |
| `mypy` strict | Success, 43 source files |
| `pytest tests` | **726 passed** (745 at module 05 included module-05 tests; this branch is from `9a1aef6`) |
| `pnpm -r test` | 54 passed |

## Four independent claims

- **Code implemented?** Yes for authoring, validation, compilation and per-run fixture instances.
- **Tests passed?** Yes — 726, including every negative case the module lists.
- **Real runtime worked?** Real PostgreSQL for fixture instances. **No screen reader.** These are
  specifications.
- **Release ready?** No.

## Next eligible module

**Module 07 — runner enrollment, admission and desktop leases** (depends on 04 and 06).
