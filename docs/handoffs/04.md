# Handoff 04 — Authoritative journal and transactional outbox

**Result: COMPLETE** for the durable-state scope. Nothing here touches a screen reader, a browser or
an operating-system action, and one boundary is explicitly out of reach from this module: database
fencing prevents new *admission*, but it cannot stop a keystroke already in flight. That proof
belongs to modules 07 and 08.

## Scope and exact state

| Item | Value |
|---|---|
| Repository | `github.com/gnanam1990/accessforge` (public), target branch `main` |
| Branch | `feat/m04-journal-and-outbox` |
| Base SHA | `141e3de` |
| Dependencies | module 02 (reducers, canonicalization) and module 03 (identity, RLS) — both merged |
| Schema revision | migration `0003_journal_and_outbox.sql` |

## Owned and changed paths

- `packages/persistence/src/accessforge_persistence/migrations/0003_journal_and_outbox.sql`
- `packages/persistence/src/accessforge_persistence/` — `runs`, `idempotency`, `outbox`,
  `sequencer`, `recovery`, `transport`, `metrics`
- `tests/integration/test_journal_crash_matrix.py`, `tests/integration/test_evidence_sequencer.py`
- `docs/adr/0005-durability-and-the-outbox.md`

Module 03's identity migrations are untouched; module 02's reducers remain the only transition
authority.

## The crash matrix

Each row commits or rolls back at a specific instant and then inspects what a recovering worker finds.

| Crash point | Observed |
|---|---|
| Before commit | no run, no audit row, no outbox message — nothing half-happened |
| After commit, before publish | message survives and a later publisher claims it; the reference names the run and revision |
| After publish, before acknowledgement | redelivered once the claim lease lapses, with the **same** `operation_id` and `attempts` incremented to 2 |
| After action intent, no result | surfaced by `recovery.ambiguous_attempts`; the only admissible resolution is INTERRUPTED + quarantine |
| SIGKILL mid-transaction, real child process | uncommitted write discarded |

The SIGKILL case uses a genuinely separate process rather than an in-process rollback, because a
rollback models a crash faithfully for the database but not for the question of whether the operating
system kept its promise.

## Concurrency, with real connections

| Case | Observed |
|---|---|
| Two workers claiming one message | disjoint claims via `FOR UPDATE SKIP LOCKED`; the loser finds nothing rather than blocking |
| Claim held inside its lease | not stolen by a second worker |
| Two concurrent identical idempotency reservations | one operation; exactly one caller believes it created it |
| Two sequencers on one attempt | serialized; an advisory lock on the attempt key is observed in `pg_locks` |
| Distinct attempts | distinct lock keys, no contention |
| Stale caller transitioning a run | refused with `StaleRevision` |

## Evidence sequencing

| Property | Observed |
|---|---|
| Chain starts at 1 from a documented genesis hash | yes — a chain cannot be silently re-rooted |
| Canonical position assigned by the sequencer, not the producer | two producers' own sequence 1 land at canonical 1 and 2 |
| Same source record, same content | idempotent replay; one event, not two |
| Same source record, different content | **rejected** — one submission is wrong and the chain will not guess |
| Producer sequence gap | refused; records stage until contiguous |
| Closing watermark beyond the admitted tail | refused |
| Record after a watermark | refused |
| Re-closing at the same sequence | idempotent, because delivery is at-least-once |
| **Contiguous chain with one producer unclosed** | **not complete** (INV-06) |
| Required producer with no stream at all | not closed — absence is not completion |

## Commands executed and observed results

| Command | Result |
|---|---|
| `uv run ruff format --check .` / `ruff check .` | clean, 122 files |
| `uv run mypy` (5 packages, strict) | Success, 37 source files |
| `uv run pytest tests -q` | **659 passed** (603 at module 03) |
| `pnpm -r test` | 54 passed |

## Mutation checks

| Mutation | Result |
|---|---|
| Outbox message written after the state commit instead of within it | 6 tests fail |
| Source-record conflict treated as a replay | 1 test fails |
| Closing watermark accepted beyond the admitted tail | 1 test fails |
| Idempotency conflict ignored | 1 test fails |
| Caller-level revision guard removed | 1 test fails |
| Statement-level revision predicate removed | 1 test fails |
| Attempt advisory lock removed | 1 test fails |

### Two guards that were present but unprovable

Mutation testing found my **tests** wrong rather than the code, twice, and both are worth recording
because the failure mode is subtle.

**The optimistic revision check was unreachable.** `apply_transition` re-read state under a row lock
and then wrote back the revision it had just read, so the comparison could never fail — removing the
predicate failed no test at all. A guard that cannot fail is indistinguishable from an absent one.
Fixed in the design, not the test: the signature now takes the revision the *caller* last observed,
which is the real concurrency question, and a stale caller is refused. The statement-level predicate
is kept as belt and braces and its presence is asserted separately, with the unreachability recorded
rather than hidden.

**The advisory lock was indistinguishable from the unique index.** A race test expecting a lock
timeout passed with the lock removed, because the second connection blocked on the `canonical_event`
primary key instead. The test now observes the lock directly in `pg_locks`. The constraint would still
catch a collision, but as a crash rather than as serialization.

## Database-level invariants

Enforced in the schema as well as in the reducers, because the reducers do not see every path:

- admissible status/outcome pairs — CHECK constraint
- an interrupted run must record why — CHECK constraint
- cancellation request and its revision are set together — CHECK constraint
- terminal runs are immutable — BEFORE UPDATE OR DELETE trigger, tested for both
- one attempt per (run, lease epoch) — UNIQUE, which is INV-10 in schema form
- one exact child authorization per run — UNIQUE
- a child must record its issuing service identity — CHECK on a non-blank value
- all eleven new tables carry FORCE row-level security

## Durable APIs available to later modules

- **Projects (05):** `runs.create_run`, `runs.load_run`, `runs.apply_transition`
- **Runner control plane (07):** `runs.start_attempt`, `recovery.ambiguous_attempts`,
  `outbox.claim_jobs`, `outbox.release_job`
- **Evidence ingestion (10):** `sequencer.admit_record`, `sequencer.close_producer_stream`,
  `sequencer.producer_tails_closed`, `sequencer.chain_is_contiguous`
- **Outcome verification (11):** `producer_tails_closed` and `chain_is_contiguous` feed
  `EvidenceValidity` directly — both are required, and neither substitutes for the other
- **Operations (26):** `metrics.queue_health`, `recovery.abandoned_jobs`

## Deliberately not done

- **No AWS transport.** `AwsSqsTransport` raises. Provisioning a queue is billable and needs
  authority this module does not have; a flag that silently degraded would become a claim that AWS
  delivery was tested.
- **No local action gating.** Database fencing stops new admission, not an action already dispatched.
  The module's own acceptance criteria say no test here may infer otherwise; modules 07/08 own it.
- **No HTTP surface.** Module 18.

## Four independent claims

- **Code implemented?** Yes for the durable journal, idempotency, outbox, jobs, sequencer, recovery
  and metrics.
- **Tests passed?** Yes — 659 Python tests including a crash matrix and real two-connection
  concurrency, with mutation proof that each durability guard bites.
- **Real runtime worked?** Partly. Real PostgreSQL transactions, real separate processes, real
  concurrent connections. No screen reader, no browser, no operating-system action.
- **Release ready?** No.

## Missing evidence and external gates

Unchanged: actual VoiceOver BLOCKED, actual NVDA BLOCKED (no Windows host), model access BLOCKED,
object store configured but not running, AWS transport unimplemented by choice. Branch protection
still not configured — an owner action.

## Next eligible module

**Module 05 — authorized projects and immutable build manifests** (depends on 03 and 04, both
available). It should gate configuration on `PROJECT_CONFIGURE` and use `workspace_connection` plus
these durable APIs rather than introducing a second persistence discipline.
