# Handoff 11 — Independent deterministic outcome verifier

- **Module:** 11 (depends on 06 and 10)
- **Requirements:** FR-006, FR-007, FR-011, FR-023
- **Invariants:** INV-01 through INV-06, INV-11, INV-12, INV-16

## What a PASS from this verifier establishes

Attached to every verdict as `PASS_SCOPE_STATEMENT`, not kept in documentation, because a verdict
travels into exports and dashboards and the caveat has to travel with it:

> This result describes one journey, executed once, on one pinned reader, browser, locale and
> keyboard layout, against one build of one environment. It establishes that the frozen assertions
> held for that execution. It does **not** establish that the application is usable by people with
> disabilities in general, that it works with other assistive technologies or other versions of this
> one, that untested journeys behave the same way, or that any legal or regulatory obligation is
> satisfied. Actual screen-reader automation supplements evaluation by disabled users and
> specialists; it does not replace it.

## Where the truth table lives, and what this module adds

Module 02 owns the precedence. This module owns everything around it:

| Piece | File |
|---|---|
| Identity revalidation, before anything else | `evaluation/identity.py` |
| Assertion values with provenance and evidence references | `evaluation/assertions.py` |
| The independent completion observer | `evaluation/observer.py` (protocol), `persistence/evidence/observer.py` (real) |
| Verdict assembly, scope, finding support | `evaluation/verdict.py` |

## Identity before observations

`revalidate` runs first and feeds `EvidenceValidity`, so a mismatch never reaches the truth table.
An evaluator that checked identities afterwards would compute a verdict and then decide whether to
believe it, and the natural failure of that shape is a plausible result reported against the wrong
build. A test asserts the ordering directly: every assertion TRUE, one identity changed, INCONCLUSIVE.

Every identity in `ALWAYS_REQUIRED` is parametrised — eight changed-identity tests and eight
missing-identity tests — rather than the two or three an author would have remembered.

Three failures are kept distinct because they lead somewhere different: **never sealed** (nothing was
promised), **not reported** (the promise cannot be checked), **different** (the promise was broken).
Collapsing them into "identity mismatch" sends someone to compare two digests when one was never
recorded.

`MODEL` is optional but not ignorable. A scripted navigator legitimately has none; a run reporting a
*different* model than the one sealed is not the approved run, and accepting it silently is how a
substitution lands.

## Provenance travels with every value

### Current continuation: retained final evaluation

The operator can now finish a normally stopped, FINALIZING manual attempt from its original five
retained execution artifacts, without supplying a verdict or replacement expectations:

```sh
uv run python -m accessforge_orchestrator.finalize_execution \
  --workspace-id WORKSPACE_UUID --run-id RUN_UUID
```

Use the private product database and evidence-store environment configured for the artifact worker.
The worker binds object keys to the exact workspace/run/attempt before reading, checks bounded
retained bytes against original closed canonical sources and the journal, and reloads the frozen
assertion contract. Missing or corrupt artifacts refuse completion. The supported bundle currently
requires exactly five artifacts; optional additional diagnostic artifacts are not supported here.

Migration0037 adds an immutable, RLS-scoped evaluation snapshot. Completion, audit/outbox, snapshot,
sealed identity bindings and attempt end commit together; an injected failure rolls them all back.
Replay returns the original snapshot, not a new assessment of current artifact availability.
The EVIDENCE_READ-gated `GET /v1/workspaces/{workspace}/runs/{run_id}/evaluation` returns its digest,
recorded timestamp and explicit ORIGINAL_EVALUATION_SNAPSHOT meaning with no-store caching. Existing
exports now preserve its original reasons/version/scope while separately checking current retention.
This does not add full offline verdict recomputation to the export format.

This admission path currently produces INCONCLUSIVE: only the evaluator version and original
assertion-set content are independently established. Physical build/profile/model/source/fixture
observations and physical execution preflight are still absent. Stored control-plane admission
and seal values are not substituted for observed runtime proof, even when a task condition is FALSE.
Trusted physical identity/probe ingestion is the next integration, not completed by this snapshot.

Verification:4 focused finalization cases,32 migration/interruption cases and5 export regressions
passed against disposable PostgreSQL/MinIO resources; reader/journal fixtures remain synthetic.
Cases include incomplete/corrupt artifacts, transaction rollback, exact replay, RLS, authentication,
immutable rows and audit/outbox cardinality. Contract/client generation now contains86 operations.
No full local suite or real screen-reader run was performed.

### Continuation: original frozen evaluation predicates

Descriptions were not executable matchers, and the stored reviewer summary previously contained
only required-assertion prose. New journey versions now store the full canonical assertion set in
`reviewer_summary.assertionContract`, covered by the existing immutable journey-version trigger.
`load_assertion_contract` checks that original content against both the version and the run's sealed
assertion digest. There is no migration, rewrite, inference from prose or legacy backfill.

The authoring API accepts an optional `evaluationRule` on an assertion:

```json
{"type":"EXACT_READER_PHRASE","actionSequence":3,"phrase":"Email invalid"}
```

This is permitted only for REQUIRED_ANNOUNCEMENT. It compares one retained captured utterance after
the specified action literally and case-sensitively, not an inferred speech window or fuzzy
semantic match. Missing/conflicting records, UNKNOWN capture, missing reference or redacted content
remain UNKNOWN. A known different utterance is FALSE for this exact predicate, not a general claim
that no announcement occurred. The pure helper labels its values EVALUATOR_DERIVED, never OBSERVER_AUTHORED.

```json
{"type":"EFFECT_COUNT","effect":"CREATE_TEST_REQUEST","count":1}
```

This is permitted only for TASK_COMPLETION. The independent observer evaluates it against its own
final post-STOP measurement and retains `assertionObservations` inside the authenticated effect
source record. The source binds `assertionSetDigest`; conditions are OBSERVER_AUTHORED and do not
include expected counts/phrases. Nonfinal receipts do not assert task completion. Missing predicate
or unavailable count is UNKNOWN. Legacy receipts replay as originally retained, not upgraded.

Rules are closed and bounded: no regex/code/selectors as an execution mechanism, unsupported rule
kinds/fields, coerced booleans/fractions or action positions beyond the journey's frozen budget.
Predicate changes alter assertion and journey digests but not navigator policy. The full contract,
including optional assertions, stays on the protected reviewer/observer side; expected literals
are not copied into the navigator policy. Rule-less canonical assertions retain their old digest
shape; they cannot gain executable meaning merely by being loaded by newer software.

Verification:100 focused rule/DSL unit cases and33 selected authoring/observer/STOP/artifact
integration cases passed. A separate real observer connection-failure case retained UNKNOWN while
the STOP handoff remained FINALIZING/NOT_EVALUATED. Changed9 Python files pass Ruff/mypy;
OpenAPI/clients still match85 operations. Journals/reader samples are synthetic; PostgreSQL and
MinIO interactions are real and isolated to test-owned resources. No repeated full local suite.

Still pending: final evaluator admission/persisted verdict and evidence identity revalidation,
UI authoring for these predicates, typed focus/order/effect-monitor/functional predicates and their
trusted evidence producers, deployed isolation and actual reader/matched repair proof. No run has
been terminalized or marked PASS by this continuation.

The evaluator **may** derive a reader assertion from a retained transcript — that transcript is the
reader's own evidence, and CONTRACTS section 7 permits it. The evaluator **may not** derive an
application observation, because no retained artifact implies "the backend received exactly one
request"; a derived value there is the evaluator inventing a backend observation and presenting it as
one. `assert_provenance_permitted` refuses the second, and the test covers the whole assertion
vocabulary rather than the single case the prompt names.

Three constructor guards make a value unusable if it is not reviewable:

- an **absent** observation must be UNKNOWN — reading it as FALSE turns a capture failure into a
  reported accessibility defect (INV-02);
- a definite value needs an **evidence reference** — a FALSE nobody can trace is not reviewable;
- an UNKNOWN needs a **reason** — "we do not know" is only useful to a reader told what was not known.

An assertion with no submitted value stays in the required set as UNKNOWN rather than being dropped.
Dropping it would shorten the list of conditions a PASS has to satisfy, which is exactly the direction
a repair must not be able to push the evaluation (INV-16).

## Completion is measured, not read

### Current continuation: independent measurement worker

`accessforge_orchestrator.completion_observer` now connects the existing ApplicationObserver to
canonical EFFECT_RECEIPT admission. It runs separately from the supervisor with operator-supplied
product and application database configuration; no supervisor token, browser session, navigator
input or reader output supplies an application count.

The worker requires an accepted, unrevoked exact manual handoff and current consent/profile/lease.
Its locally configured observer reference must match the sealed environment, whose actual fields
are rehashed. The per-run fixture must have its intact creation-time contract and a template digest
matching the sealed fixture. This first implementation supports `CREATE_TEST_REQUEST` only.

After a settled action boundary it releases product locks, reads the reference application's own
`public.fixture_instance` and `public.service_request` in one read-only REPEATABLE READ snapshot,
and verifies the application's exact nonce/template before counting. Connect and statement budgets
are5seconds; lock waits are bounded to1second. A missing/mismatched fixture or unavailable database
is UNKNOWN with a null count, not a measured zero. SQL/connection exception details are not retained.

Before evidence admission, it rechecks current authority, fixture and the action boundary under
locks. An intervening action, revocation, epoch or fixture change refuses admission. The existing
sequencer receives an OBSERVER-only source record with fixture instance ID, action boundary,
measurement time, count and digest; fixture values, nonce and credential values do not enter it.
A stable source UUID is idempotent: replay returns the original retained observation without
reading the application again. It cannot present an earlier action/fixture boundary as a new one.
There is a128-record-per-observer/attempt budget. The stream is left open for explicit closure.

#### Operator command

Provide `ACCESSFORGE_DATABASE_URL` and `ACCESSFORGE_OBSERVER_DATABASE_URL` through the worker's
private service environment. The second must be the separately configured application database,
using a read-only observer role. Resolve the opaque sealed credential reference through trusted
deployment configuration; never take a DSN or reference-to-secret mapping from the navigator.

```sh
uv run python -m accessforge_orchestrator.completion_observer \
  --workspace-id WORKSPACE_UUID --run-id RUN_UUID \
  --observer-credential-ref SEALED_OBSERVER_PROFILE \
  --source-record-id STABLE_MEASUREMENT_UUID
```

Exit0 means a known measurement was retained,3 means UNKNOWN was retained, and1 means refused or
unavailable control-plane admission. Exit2 is argument/configuration error. None means run PASS.
Reuse the source UUID only to
recover acknowledgement of that same measurement; use a fresh UUID for a new authorized sample.

Validation:9 focused real PostgreSQL cases, including the actual CLI, passed. Cases cover one/zero
rows, missing fixture, wrong template, unreachable application, wrong observer reference, and
revocation or a new action during the read. Replay is checked with an unreachable application.
Test-only submitted rows are synthetic; measurement, transactions and persistence are real.
Each check owns separate disposable product/application databases. Changed Python static checks
passed; no repeated full-suite run or disabled assertions.

Not deployed: this command does not provision least-privilege roles, attest process isolation,
install a secret resolver or grant a supervisor an observer credential. Those operator/deployment
boundaries still need verification. Completion assertions from frozen expectations, retained
artifact intake/final verdict admission and actual reader/matched-repair proof remain pending.

STOP continuation: pass `--final` only after the exact attempt's successful STOP result. The worker
revalidates that settled boundary after reading, marks finalSample in the immutable source, and
closes only its own producer stream atomically with admission. An earlier nonfinal source UUID
cannot be upgraded to final. The supervisor cannot close this stream on its behalf. Final does not
mean KNOWN or PASS; a failed measurement stays UNKNOWN. Seven focused STOP integration cases cover
the worker's final receipt, missing observer/tail, absent or unresolved STOP, revocation and rollback.

The observer counts rows the application actually holds, keyed on this run's fixture nonce. It reads
the application's own database — not an HTTP endpoint it serves, because an endpoint returns what the
application chooses to say, and not the DOM, because the DOM is what the page chose to display.

The integration test executes the failure this exists to prevent: nothing is submitted, and the
observation is FALSE. In a real run of the inaccessible variant every reader assertion would be
correctly TRUE, because the page renders a success message having persisted nothing.

**Read-only at the database.** `SET TRANSACTION READ ONLY`, so an attempted write fails rather than
relying on this class not containing one. The realistic way an observer gains a write is someone
adding a `reset_fixture` helper to it — it already holds a connection to the application and it looks
tidy — and a read-only transaction makes that attempt fail instead of succeed. A structural test
asserts the class exposes exactly one public method.

**Unavailability is UNKNOWN, never zero.** A connection refused and "the application holds no
requests" are opposite conclusions; returning zero would report a confirmed task failure every time
the network hiccuped.

**A prior fixture's receipt does not count.** Effects are counted per nonce. Without that, one
successful run would satisfy every later run's completion assertion for as long as the row survived.

**The observation carries no oracle material.** `CompletionObservation` has no `expected` field, so a
caller holding one cannot read what the count was compared against.

## A property of the reference application, found by trying to test around it

The first version of the double-submission test inserted two rows to check that two reads as FALSE.
It could not: the reference application holds a unique index on `fixture_nonce`, so a second
submission for one fixture is refused by its own database.

That is asserted now rather than worked around. The observer's *exactly one* comparison is a check on
a count the application cannot make exceed one — so a run reporting two would be evidence of something
much stranger than a double click. The comparison stays, because an observer that only checked "at
least one" would accept a state the application considers impossible without anyone noticing.

## Findings

Only a complete, valid FAIL supports `REPRODUCED`. An INCONCLUSIVE run containing a FALSE observation
supports `CANDIDATE` and nothing stronger: incomplete evidence cannot distinguish a real failure from
a consequence of what was missing. A PASS supports nothing — there is no such thing as a finding that
something worked.

## Test fixtures are independent of the code under test

The prompt asks for "independent offline/unit test fixtures not generated by the same code path as
evaluation", and the reason is that a fixture built by calling the thing under test agrees with it by
construction. The identity table, the assertions and the observer stubs in `test_outcome_verifier.py`
are all written out by hand.

## Mutation checks

| Mutation | Result |
|---|---|
| Identity mismatch no longer blocks | 8 tests fail |
| A supervisor may author an application observation | 3 tests fail |
| An unreachable observer reports zero rather than unknown | 2 tests fail |
| A prior fixture's receipt is accepted | 1 test fails |
| An inconclusive run supports REPRODUCED | 1 test fails |

Restores verified byte-identical.

## Verification

| Check | Result |
|---|---|
| `pytest tests` (fresh database, live MinIO, real reference-app database) | **1118 passed** |
| `ruff` / `mypy` strict | clean, 61 source files |

## What this does not establish

**No real screen-reader trace has been evaluated.** The prompt's acceptance criterion — "real
VoiceOver trace evaluation yields explainable results" — is **unmet**, and cannot be met until module
08 is unblocked. Every reader assertion value in these tests was written by the test.

What *is* proved end-to-end is the completion observer: it reads a real PostgreSQL database belonging
to the real reference application, and correctly reports FALSE for a submission that did not happen.
That is the half of the verdict that does not require a screen reader, and it is the half that catches
the most dangerous failure — a page that announces success having done nothing.

## A CI gap the independence assertion exposed

The first CI run of this module failed on nine errors: `REFAPP_DATABASE_URL is not configured`.

Locally the reference application has its own database (`accessforge_refapp`); in CI it had been
sharing the product's. The existing reference-app journey test never noticed, because it passes the
product's URL to the application's settings and both work.

Sharing would have made `test_the_observer_reads_the_application_not_the_product` vacuous — it would
be comparing a URL to itself. It is also the arrangement that produced module 06's silent table
collision, where the product's `fixture_instance` was the name the application already used in the
same database. CI now creates `accessforge_refapp` owned by the non-superuser test role, so the two
sources are genuinely different.

## Next

Module 12 (Strands navigator) needs AWS Bedrock credentials and separate approval for billable model
invocation; both are owner blockers. Modules 16 (human review), 18 (HTTP API), 19 (events and
schedules) and 21–24 (UI) are buildable without a reader or a model.
