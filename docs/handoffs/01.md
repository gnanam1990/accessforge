# Handoff 01 — Reproducible workspace foundation

**Result: COMPLETE for everything not gated on an external capability.**
The workspace, the reference application, the test ladder and CI are real and were executed. The
screen-reader boundary remains BLOCKED from module 00 and nothing here pretends otherwise.

## Scope and exact state

| Item | Value |
|---|---|
| Repository | `github.com/gnanam1990/accessforge` (public), target branch `main` |
| Branch | `feat/m01-workspace-foundation` |
| Base SHA | `fc00eb8` (merged module 00) |
| Dependencies | module 00 COMPLETE — `docs/handoffs/00.md`, capability evidence in `docs/capabilities.md` |

## Owned and changed paths

- Workspace manifests: `pyproject.toml`, `uv.lock`, `package.json`, `pnpm-lock.yaml`,
  `pnpm-workspace.yaml`, `tsconfig.base.json`, `.gitignore`
- Configuration templates: `.env.example`, `.env.refapp.example`, `.env.test.example`
- Control plane: `apps/api/` (configuration, health, entrypoint)
- Reference application: `fixtures/reference-app/` (config, storage, validation, templates, HTTP)
- TypeScript targets: `apps/desktop-runner/`, `apps/web/`
- Boundary markers: `packages/*/README.md`, `apps/orchestrator`, `apps/build-worker`, `infra/`
- Tests: `tests/unit/`, `tests/integration/`, `tests/conftest.py`
- CI: `.github/workflows/ci.yml`
- Docs: `docs/adr/0002-configuration-and-workspace-boundaries.md`, `docs/development/VERIFICATION.md`

Not touched: `specs/accessforge/` (unchanged, CI enforces this).

## Contract and ADR changes

No contract changes. ADR 0002 records the environment-file split, the reference application's
design, and the ownership-marker approach.

## Commands actually executed and observed results

| Command | Result |
|---|---|
| `uv sync --frozen` | resolved; `uv.lock` committed |
| `pnpm install --frozen-lockfile` | resolved; `pnpm-lock.yaml` committed |
| `uv run ruff format --check .` | 78 files already formatted |
| `uv run ruff check .` | All checks passed |
| `uv run mypy apps/api/src fixtures/reference-app/src` | Success: no issues in 12 source files (strict) |
| `uv run pytest tests -q` | **69 passed** (53 unit, 16 integration) |
| `pnpm -r --if-present typecheck` / `build` | Done, both packages |
| `pnpm -r --if-present test` | 2 passed, 0 failed |

Runtime, exercised over real HTTP against real PostgreSQL 17.10:

| Step | Observed |
|---|---|
| Reference app start | pid recorded; `/health/live` → `{"status":"alive"}` |
| Readiness with database up | 200 `{"status":"ready","dependencies":{"postgresql":"ok"}}` |
| Fixture creation (setup identity) | 201, nonce + `template_digest` `bb04b651…01ea` |
| Receipt without token / with setup token / with observer token | **403 / 403 / 200** |
| Invalid submission | **422**, "Enter an email address in the format name@example.com."; `request_count` stayed **0** |
| Corrected submission | **201** "Request received", one row with a UUID |
| Resubmission | **409**, `request_count` still 1 |
| Backend restart (kill, restart, re-read) | `request_count` = 1, same request id — persistence survived |
| API readiness with no object store | **503**, `evidence-store: ConnectionRefusedError`, while liveness stayed 200 |
| `/diagnostics` on both services | every credential rendered `<redacted>` |

Only the processes started for this task were stopped. PostgreSQL was left running.

## Regression and failure cases proved

Mutation checks against high-risk guards, run in the working tree and restored from a pristine
copy verified byte-identical afterwards:

| Mutation | Result |
|---|---|
| Authorization always allows | **7 integration tests failed** |
| Email validation removed | **7 tests failed** |
| Readiness always reports ready | **1 test failed** |
| Inaccessible variant silently healed | **7 unit tests failed** |
| Loopback binding guard removed | **4 unit tests failed** |
| Restored candidate | 69 passed; `diff -r` against pristine showed no differences |

**A correction worth recording.** The first mutation attempt ran in a copied worktree whose
editable installs still pointed at the original source tree, so the mutants were never imported
and all five "passed". That was a check proving nothing. It was redone in the real tree with a
verified restore, and only those results are reported above.

Three defects were found by these tests and fixed before commit:

1. **Authorization was not running.** Declaring the token dependency as `Annotated[None, Depends(...)]`
   made FastAPI treat it as a request field, so an unauthenticated call was rejected as malformed
   (422) before the authorization dependency was consulted. Moved to route-level `dependencies=[...]`.
2. **Placeholder detection used equality**, so `REPLACE_ME_WITH_A_RANDOM_VALUE` — the exact value
   shipped in the example files — would have been accepted as an observer token. Now substring-based.
3. **`.gitignore` would have excluded two of the three `.env*.example` templates**, leaving a fresh
   clone with no template for them. Pattern changed to `!.env*.example`.

## Real platform evidence

Real PostgreSQL 17.10 (`PostgreSQL 17.10 (Homebrew) on aarch64-apple-darwin24.6.0`), real HTTP,
real process restart. **No screen reader was involved at any point**, and nothing in this module
produces or implies accessibility evidence. `test_a_receipt_is_not_an_accessibility_claim` asserts
the success page is byte-identical across the accessible and inaccessible variants, so a receipt
structurally cannot carry an accessibility signal.

Object storage was configured but not run; the API's readiness failing with 503 is the honest
report of that, not a defect.

## Deviation from the module's own instructions

The prompt asks for test-first development. Validation, configuration and template code were
written before their tests, then tests were added and the guards mutated to prove the tests fail
when the behaviour breaks. The mutation results give the same falsifiability guarantee test-first
is meant to provide, but the ordering was not followed and is recorded rather than glossed.

## Security and privacy implications

Loopback binding is enforced by configuration for the reference application; non-loopback hosts and
non-local database URLs are rejected. Observer and setup identities are distinct and neither
substitutes for the other. Credentials never appear in diagnostics or dependency-failure output —
health checks report exception class names only, because driver messages can embed connection
strings. No `.env` file is committed. The database role is not a superuser. The CI workflow uses
`permissions: contents: read`.

The reference application performs no outbound network calls, sends no email, takes no payment,
and cannot submit anywhere external.

## Missing evidence and external gates

| Gate | Status |
|---|---|
| Remote CI | Defined in this PR; **its first real run is on this pull request**. No prior CI has ever passed |
| Actual VoiceOver | BLOCKED — not configured on this host (module 00 §5.1) |
| Actual NVDA | BLOCKED — no Windows host |
| Object store integration | Configured, not running; module 10 owns the real contract |
| Model access | BLOCKED — no verified credentials |
| Branch protection | Not configured; owner action |

## Four independent claims

- **Code implemented?** Yes, for module 01's scope: workspace, control-plane skeleton, reference
  application, test ladder, CI.
- **Tests passed?** Yes — 69 Python and 2 Node tests, executed, with mutation checks proving the
  high-risk ones fail when the behaviour breaks.
- **Real runtime worked?** Yes — real HTTP against real PostgreSQL, including restart durability
  and an honest readiness failure. No screen reader was involved.
- **Release ready?** No. E0 needs actual VoiceOver, a model endpoint, and modules 02–17.

## Next eligible module

**Module 02 — canonical schemas and deterministic reducers** (`packages/contracts`,
`packages/domain`). Depends only on module 01 and needs no blocked capability.
