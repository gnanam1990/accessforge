# `infra/`

**Owned by module 27 (CI, deployment preparation and restore). Not implemented.**

Deployment, runner bootstrap and restoration procedures land here. Local development currently
needs only a running PostgreSQL server and, once evidence objects exist, an S3-compatible store;
both are documented in `docs/development/VERIFICATION.md`.
