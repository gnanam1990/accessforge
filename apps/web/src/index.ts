/**
 * Web workspace entrypoint.
 *
 * The accessible product interface is module 21's responsibility, and the screens that consume it
 * belong to modules 22–24. This file exists so the TypeScript workspace has a real build target.
 * There is no placeholder UI here: a mockup rendered now could be mistaken for a working product.
 */
export const WEB_UI_OWNER_MODULE = 21;
